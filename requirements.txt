# Core dependencies for Universal Document Extractor Apify Actor
# Uses Google Gemini 2.0 Flash for vision and extraction (no OCR libraries)

# Apify SDK
apify==2.7.1

# Google GenAI (Gemini 2.0 Flash)
google-genai==1.0.0

# Pydantic v2 for schema validation and business logic
pydantic>=2.8.0

# HTTP client (used by Apify SDK)
httpx==0.27.0

# Development and testing
pytest==8.3.3
pytest-asyncio==0.23.7
